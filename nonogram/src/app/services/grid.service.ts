import { Injectable } from '@angular/core';
import { BehaviorSubject } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class GridService {

  // private changed= new BehaviorSubject<Item[]>([])
  // list = this._list.asObservable();

  constructor() { }
}
